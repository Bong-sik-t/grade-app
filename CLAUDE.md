# 1학년 페이지 프로젝트 노트

경남고등학교 1학년부 관리 앱. 순수 HTML/CSS/JS + Firebase Realtime Database. Vercel 배포.

## 파일 구조
- `index.html` — 교사용 대시보드 (메인 앱, 6000줄+)
- `student.html` — 학생용 야자 신청 페이지 (`/student` 경로)
- `summer-student.html`, `summer-qr-attend.html` — 여름방학 자습 학생용 (현재 실데이터 없음, 미사용 상태)
- `firebase-ready.js` — Firebase 연동 헬퍼
- `code.gs.txt` — 레거시 Google Apps Script 백엔드. 아직 배포는 살아있지만(3월분까지만 응답) 현재 운영은 100% Firebase 기준이라 건드릴 필요 없음
- `design/gn1-redesign-mockup.html` — UI 리디자인 목업 (아래 참고)

## 최근 작업 로그 (2026-07-24)

1. 야간 자율학습 탭에 "학생 신청 페이지 / URL 복사" 버튼 추가 (여름방학 탭과 동일 패턴)
2. 학생 상태 변경 모달에 "기숙사 학생" 체크박스 추가 — 교사가 직접 토글 가능해짐
3. **석식 신청 기능 전체 삭제** — index.html(탭·모달 2개·함수 15개+·Firebase 저장/복원 파이프라인)과 student.html(신청 흐름) 양쪽에서 완전 제거. 석식은 별도 시스템으로 이관되어 앱 내 기능 불필요.
4. **기숙사 학생 8교시 자동 적용 로직 정리**
   - student.html에 기숙사 인식 추가 — 학생이 직접 신청할 때 자동으로 8교시만 적용, 야자1·2는 안내 배너로 대체
   - index.html에서 실제 버그 발견 및 수정: 기숙사 학생의 8교시 자동 적용이 상단 통계·반별 총계·출석 확인 요약·**인쇄되는 야자 명단(`printYajaRoster`)**에는 반영이 안 되고 있었음 → 6곳 일관되게 `isDormAutoP8()` 참조하도록 수정
   - 반별 입력 화면: 기숙사 학생 8교시 체크박스를 **체크됨+비활성**으로 표시(자동 적용을 시각적으로 확인 가능하도록)
5. **GAS(Google Apps Script) 레거시 코드 검토** — `migrateFromGAS()`는 Firebase가 완전히 비어있을 때만 실행되는 1회성 안전장치. 실제 GAS 배포는 살아있지만 데이터가 4개월 이상 뒤처짐(attendCheck 3월분까지만, 지금은 7월). 상의 결과 **삭제하지 않고 그대로 유지**하기로 결정. 자동 백업 추가는 보류 상태.
6. **여름방학 자습 탭 숨김 처리** (index.html nav, 493행 부근) — 실사용 데이터가 전혀 없어(감독 0명·신청 0건) 메뉴에서만 제외. 코드·데이터는 그대로 남아있고 nav-tab 주석만 해제하면 즉시 복원됨.
7. **UI 리디자인 목업 제작** (`design/gn1-redesign-mockup.html`, Claude Artifact로도 게시됨)
   - 데스크톱: 사이드바를 "자주 사용"(홈·야자·감독표·학생관리) / "그 밖의 업무"로 그룹화, 홈 대시보드 신설
   - 모바일: 하단 탭바(홈·야자·감독표·더보기) + 더보기 시트, 학생 관리는 표 대신 카드 목록으로
   - 실제 Firebase 데이터로 숫자 대입 완료 (학생 185명, 7월 야자 신청 78/182, 기숙사 22명 등 — 학생 개인 실명은 프라이버시상 가상 이름 사용)
   - Artifact 링크: https://claude.ai/code/artifact/4d31f0b9-1758-46f9-b627-32569946d490 (새 세션에서도 열람 가능. 이어서 수정하려면 이 md의 로컬 파일을 직접 읽고 편집한 뒤 같은 파일 경로로 재게시하면 URL 유지됨)

## 다음에 이어서 할 일 (미정 사항)
- 목업 방향 최종 확인 → 실제 index.html에 반영할지, 반영한다면 어디부터 착수할지 결정
- 여름방학 자습 기능: 완전 삭제할지 이번 학기 다시 쓸지 아직 미정 (일단 숨김 처리만 해둠)
- 학생 데이터 자동 백업 체계: 필요성 논의는 했으나 보류 상태 — 나중에 다시 꺼내도 됨
